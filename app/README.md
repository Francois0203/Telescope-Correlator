Main application (production) for the Telescope Correlator.

Structure:
- `src/` - Python packages and modules implementing the correlator.
- `Dockerfile` - Container recipe (added after approval).
- `configs/` - Runtime YAML/JSON configs.

This directory is intentionally left with placeholders until you approve the repo structure.
