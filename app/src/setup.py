from setuptools import setup, find_packages

setup(
    name="telescope-correlator",
    version="0.0.0",
    description="Telescope correlator package",
    packages=find_packages(),
    include_package_data=True,
)
