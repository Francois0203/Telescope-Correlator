.venv
.git
.gitignore
.github/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# validation/ stays in the build context because validation/Dockerfile needs
# it. It is not copied into the correlator image — app/Dockerfile only takes
# requirements.txt and app/src/ — so the runtime image is unaffected.

# Reports written by the validation container.
validation/reports/

# Test data and outputs
tests_harness/test_data/
dev_workspace/outputs/
*.npy
*.png

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Documentation
*.md
!README.md

# Environment files
.env
.env.local
.env.*.local
