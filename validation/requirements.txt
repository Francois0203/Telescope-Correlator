# Tier 2 only. Tier 1 (the analytic oracle) needs nothing beyond the
# correlator's own numpy/scipy, so the whole file is optional.
#
#     pip install -r validation/requirements.txt
#
# These are heavy (astropy pulls in a large dependency tree) and are
# deliberately kept out of the correlator's requirements.txt and its Docker
# image. Nothing in app/src imports them.

# The [sim] extra pulls in mpi4py, which pyuvsim.uvsim requires even for a
# single-process run. mpi4py needs an MPI implementation at build time
# (libopenmpi-dev in validation/Dockerfile). That is why this tier runs in a
# container: on Windows it would mean installing MS-MPI by hand.
pyuvsim[sim]>=1.4
pyuvdata>=3.0
pyradiosky>=1.0
astropy>=6.0
